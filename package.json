{
  "name": "wealth-compass",
  "version": "1.0.0",
  "private": true,
  "description": "Wealth Compass — financial-relationships assessment (static app).",
  "scripts": {
    "build:css": "tailwindcss -i styles/tailwind-input.css -o styles/tailwind.css --minify",
    "watch:css": "tailwindcss -i styles/tailwind-input.css -o styles/tailwind.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
